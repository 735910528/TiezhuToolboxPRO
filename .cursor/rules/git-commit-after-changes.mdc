---
description: 功能改动完成后及时 git 提交，方便出问题回退
alwaysApply: true
---

# 改完即提交

本仓库二次开发时，**每完成一个可验证的改动点就 git commit**，不要攒一大包再提。

## 何时提交

- 一项功能、修复或重构已经能编译/冒烟通过，或用户明确说「改完了 / 提交」
- 同一会话内若继续改别的主题，先把上一主题提交掉再开下一刀
- 新建或修改 `.cursor/rules`、关键配置后也一并提交

## 提交要求

- 先 `git status` / `git diff` / `git log`，再按仓库现有风格写说明（常用 `feat:` / `fix:` / `chore:` 等中文说明）
- 只暂存与本次改动相关的文件；勿提交密钥、本机路径隐私、`bin/`/`obj/`/`screenshots/` 等
- **不要** `push`，除非用户明确要求推远程
- **不要** `amend` /强制推送，除非用户明确要求且符合安全条件
- 提交后跑一次 `git status` 确认干净或仅剩无关文件

## 回退提示

出问题时可用 `git log` 定位提交，再用 `git revert <hash>` 或 `git checkout <hash> -- <path>` 回退；因此提交要小而清晰，说明写清「为什么」。
